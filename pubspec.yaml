name: simple_value_object_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/r-koubou/dart_simple_value_object_generator.git

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  # extension types: available since Dart SDK 3.3
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  simple_value_object_annotation:
    git: https://github.com/r-koubou/dart_simple_value_object_annotation.git
  build: ^4.0.3
  source_gen: ^4.1.1
  analyzer: ^9.0.0


dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
  build_runner: ^2.10.4
